import { Box } from "@chakra-ui/react";
import ComicList from "./ComicList/ComicList";

const AppContainer = () => {
  return (
    <Box pl="1rem">
      <ComicList />
    </Box>
  );
};

export default AppContainer;
